<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.graphvisualiser">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GraphVisualiser"
        android:usesCleartextTraffic="true">

        <activity android:name=".SplashscreenActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.GraphVisualiser"/>

        <!-- used for google ocr text bounding boxes, but i'm not using them because its unreliable
        <meta-data           for numbers and symbols
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="ocr" />
        -->

        <service
            android:enabled="true"
            android:name=".model.ModelInferenceService" />
    </application>

</manifest>